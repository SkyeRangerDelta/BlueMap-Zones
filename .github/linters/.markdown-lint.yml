---
default: true

# Prose is not hard-wrapped in this repo.
MD013: false
# Duplicate headings are fine across different sections (e.g. changelog versions).
MD024:
  siblings_only: true
# CHANGELOG.md is generated by semantic-release and does not follow all rules.
MD034: false
